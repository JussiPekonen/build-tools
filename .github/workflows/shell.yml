name: shell
on:
  push:
    paths:
      - 'shell/**'

jobs:
  shellcheck:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v3
      - name: setup
        run: cmake . -Bout
      - name: shellcheck
        run: make -Cout shellcheck
